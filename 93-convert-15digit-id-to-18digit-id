public String convert15to18DigitId(String fifteenDigit)
{
    //code here
    return  String.isBlank(fifteenDigit) ? '-1' : fifteenDigit.length()< 15 ?'-1' : (Id)fifteenDigit ;
    //typeConstructing into ID has handled the headache already for us
}
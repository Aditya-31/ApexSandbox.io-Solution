public void validateInsert(List<Opportunity> opportunities){
    //code here
    for(Opportunity oppObj: opportunities){
        if(oppObj.StageName == 'Closed Won' && (oppObj.Description == null || String.isEmpty(oppObj.Description) )){
            oppObj.addError = 'Description should not be empty for Closed Won opportunity.';
        }
    }
}
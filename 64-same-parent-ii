public Boolean sameParent(Account acc, Contact con, Opportunity opp) {
    //code here
    return (con.AccountId != null && opp.AccountId != null && acc.Id!= null && con.AccountId == opp.AccountId);
}